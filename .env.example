CONVEX_DEPLOY_KEY
CONVEX_DEPLOYMENT
CONVEX_TAVILY_API_KEY
CONVEX_GOOGLE_API_KEY
VITE_CONVEX_URL
# Convex Configuration
VITE_CONVEX_URL=your-convex-url

# Upstash Redis Configuration (Optional - for scalable sync engine)
# Frontend variables (must start with VITE_)
VITE_KV_REST_API_URL=your-upstash-url
VITE_KV_REST_API_TOKEN=your-upstash-token
VITE_ENABLE_REDIS_CACHE=false

# Sync Engine Configuration
# Set to "true" to use the new scalable sync engine with Redis support
# Set to "false" or remove to use the old corrected sync engine
VITE_USE_SCALABLE_SYNC_ENGINE=false

# Backend Redis variables (for future server-side usage)
# KV_REST_API_URL=your-upstash-url
# KV_REST_API_TOKEN=your-upstash-token